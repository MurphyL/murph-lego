{
  "name": "murph-lego",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "ajv": "^8.11.0",
    "ajv-formats": "^2.1.1",
    "axios": "^0.27.2",
    "htm": "^3.1.1",
    "path-browserify": "^1.0.1"
  },
  "scripts": {
    "build": "browserify src/index.js -t [ babelify --presets [ @babel/preset-env ] --plugins [] ] | uglifyjs > public/app.js",
    "predev": "browserify src/index.js -t [ babelify --presets [ @babel/preset-env ] --plugins [] ] > public/app.js",
    "dev": "serve public"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/core": "^7.18.6",
    "@babel/preset-env": "^7.18.6",
    "babelify": "^10.0.0",
    "browserify": "^17.0.0",
    "serve": "^13.0.2",
    "uglify-js": "^3.16.1"
  }
}
